# Git

- Ignore secrets, local environment files, dependencies, build output, caches, and `.tmp/`. Ensure ignore rules allow `.env.template`.
- Keep GitHub workflows in `.github/workflows/` and update their paths and package commands after restructuring.
- Write commit messages as past-tense actions, such as `Updated project configuration`.
