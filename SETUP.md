# Setup

This is my personal setup.

## Instructions

Firstly, run the setup script:

```bash
chmod +x ./setup.py # Allow execute permission.
./setup.py
```

Then, add this line into your `.bashrc` or `.zshrc`:

```bash
. ~/.personal
```
